spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/discodeit
    username: discodeit_user
    password: discodeit1234
    driver-class-name: org.postgresql.Driver

server:
  port: 8080

logging:
  level:
    root: debug
    com.something.discodeit: debug  # 우리 프로젝트 로그
