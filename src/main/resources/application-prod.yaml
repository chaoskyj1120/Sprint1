spring:
  datasource:
    url: jdbc:postgresql://prod-db-server:5432/discodeit
    username: prod_user
    password: prod_pass
    driver-class-name: org.postgresql.Driver

server:
  port: 80

logging:
  level:
    root: info
    com.something.discodeit: info
